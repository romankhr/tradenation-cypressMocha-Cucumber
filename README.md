# tradenation-cypressMocha-Cucumber
1. Clone project
git clone 

2. Install dependencies
npm install

3. Run tests
 npx cypress run --browser chrome

4. Open report
mochawesome-report\mochawesome.html in a browser